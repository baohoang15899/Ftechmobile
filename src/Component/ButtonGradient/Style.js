import { StyleSheet } from 'react-native'

export const Styles = StyleSheet.create({
    buttonText: {
        fontSize: 18,
        padding:10,
        textAlign: 'center',
        color: '#ffffff',
        backgroundColor: 'transparent',
      },
    linearGradient:{
        borderRadius:8,
    },
})

export const Color = {
    Arr:['#00c6ff', '#0072ff'],
}